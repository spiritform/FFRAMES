[project]
name = "fframes"
version = "0.1.0"
description = "Video frame picker + trimmer for ComfyUI. Scrub, pick FF/LF, pipe into first-frame/last-frame video models."
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
    "opencv-python",
    "pillow",
    "numpy",
]

[project.urls]
Repository = "https://github.com/spiritform/FFRAMES"

[tool.comfy]
PublisherId = "spiritform"
DisplayName = "FFRAMES"
Icon = ""
